{
  "name": "taz-crosshair",
  "version": "0.1.0",
  "description": "Configuration interface for crosshair overlay",
  "scripts": {
    "dev": "echo 'No development server needed - use Tauri directly'",
    "build": "echo 'No build step needed - using static HTML'"
  },
  "devDependencies": {},
  "dependencies": {}
} 